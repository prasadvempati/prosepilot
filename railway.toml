[build]
builder = "railpack"
buildCommand = "pnpm install && turbo build"

[deploy]
startCommand = "node services/api/dist/index.js"
healthcheckPath = "/health/live"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
